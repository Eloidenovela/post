#pragma once
#include <string>
// #include "webui.h"
#include "webui.hpp"

namespace view {
    inline std::string index() {
        return R"(

        )";
    }

    // webui::window win;
    // win.webui_show(10, index());
}